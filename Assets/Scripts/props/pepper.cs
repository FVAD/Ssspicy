using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class pepper : props
{
    // Start is called before the first frame update
    void Start()
    {
        foodKind = FoodKind.pepper;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
